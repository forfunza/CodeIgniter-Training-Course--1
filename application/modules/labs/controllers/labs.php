<?php

class Labs extends MY_Controller {
	
	public function cache()
	{
		//$this->load->model('users/model_users', 'users');
		
		
		/*
		$id = 4;
		
		$data = array(
			'password' => 'ababab'
		);
		$this->users->update($data, $id);
		

		$data = $this->users->getRecord(4);
		
		alert( $data );*/
		
		alert( userInfo(4) );
		
		alert( userInfo(4) );
		alert( userInfo(4) );
		alert( userInfo(4) );
		alert( userInfo(4) );
		alert( userInfo(4) );
		alert( userInfo(4) );
		alert( userInfo(4) );
		
	}
	
}